spring:
  profiles:
    active: dev
  config:
    import: classpath:report-config.yml

# Frontend and QR configuration
app:
  frontend:
    base-url: "http://192.168.1.10:5173/mobile"
  qr:
    size: 300
